# Cortex MCP Server — Harper Deployment
# Required for classifyMessage() when using memory_store
ANTHROPIC_API_KEY=sk-ant-...

# Harper Fabric deployment credentials (for npm run deploy)
CLI_TARGET=https://your-cluster.harpercloud.com
CLI_TARGET_USERNAME=your-username
CLI_TARGET_PASSWORD=your-password
